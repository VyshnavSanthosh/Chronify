<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify OTP - ChronoMy</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="./css/verify_otp.css">
</head>
<body class="verify-otp-page">
    <div class="container-fluid d-flex align-items-center justify-content-center min-vh-100">
        <div class="verify-otp-container">
            <!-- Page Heading -->
            <h1 class="verify-otp-title">Verify OTP</h1>
            
            <!-- Description Text -->
            <p class="verify-otp-description">
                We've sent an email with an activation code to your phone <span><%= userEmail %></span>
            </p>

            <!-- OTP Form -->
            <form id="otpForm" class="otp-form">
                <!-- OTP Input Boxes -->
                <div class="otp-inputs-container">
                    <% for (let i = 0; i < 4; i++) { %>
                        <input 
                            type="text" 
                            class="otp-input" 
                            maxlength="1" 
                            inputmode="numeric" 
                            name="otp<%= i %>"
                            id="otp<%= i %>"
                            data-index="<%= i %>"
                            placeholder=""
                            aria-label="OTP digit <%= i + 1 %>"
                            required
                        >
                    <% } %>
                </div>

                <!-- Timer and Resend Section -->
                <div class="timer-section">
                    <p class="send-code-again">
                        Send code again <span id="timer">00:20</span>
                    </p>
                </div>

                <!-- Resend Link -->
                <div class="resend-section">
                    <p class="resend-text">
                        I didn't receive a code 
                        <a href="#" class="resend-link" id="resendLink">Resend</a>
                    </p>
                </div>

                <!-- Verify Button -->
                <button type="submit" class="verify-btn">Verify</button>
            </form>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script src="/js/verify-otp.js"></script>
</body>
</html>
