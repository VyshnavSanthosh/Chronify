<!-- Header -->
<header class="h-16 flex items-center justify-end px-8 border-b border-gray-800">
    <div class="flex items-center space-x-3">
        <div class="text-right">
            <p class="text-sm font-semibold"><%= locals.brandName || (locals.user ? locals.user.firstName : 'Vendor') %></p>
            <p class="text-xs text-gray-400 uppercase tracking-tighter">Vendor</p>
        </div>

    </div>
</header>
