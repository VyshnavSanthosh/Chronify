<% layout('admin/layout/adminLayout') %>

<% block('styles', `
  <style>
    .status-badge {
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
    }
    .status-active {
      background-color: rgba(34, 197, 94, 0.2);
      color: #22c55e;
      border: 1px solid #22c55e;
    }
    .status-blacklisted {
      background-color: rgba(239, 68, 68, 0.2);
      color: #ef4444;
      border: 1px solid #ef4444;
    }
    .brand-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: linear-gradient(135deg, #4a7ba7, #5a8bc0);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 600;
      font-size: 14px;
    }
  </style>
`) %>

<!-- Content Area -->
<div class="p-8 lg:p-12 space-y-8">
  
  <!-- Page Title and Breadcrumbs -->
  <div class="space-y-4">
    <h2 class="text-4xl font-bold tracking-tight uppercase" style="font-family: 'Playfair Display', serif;">VENDOR LIST</h2>
    <nav class="flex text-sm text-gray-400 space-x-2">
      <a href="/admin/dashboard" class="hover:text-white capitalize">Dashboard</a>
      <span><i class="fa-solid fa-chevron-right text-[8px] mx-1"></i></span>
      <span class="text-white capitalize">Vendor List</span>
    </nav>
  </div>

  <!-- Filters Section -->
  <div class="flex flex-col md:flex-row gap-4 items-center justify-between">
    <!-- Search Bar -->
    <div class="relative w-full max-w-md">
      <i class="fa-solid fa-magnifying-glass absolute left-4 top-1/2 -translate-y-1/2 text-gray-500"></i>
      <input type="text" 
             id="searchInput"
             placeholder="Search Vendor. . ." 
             class="w-full bg-[#0a1425] border border-gray-800 rounded-lg py-3 pl-12 pr-4 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all">
    </div>

    <div class="flex gap-4 w-full md:w-auto">
      <!-- Status Filter -->
      <div class="relative w-full md:w-40">
        <select id="statusFilter" class="w-full appearance-none bg-[#0a1425] border border-gray-800 rounded-lg px-4 py-3 pr-10 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 cursor-pointer">
          <option value="">All Status</option>
          <option value="false">Active</option>
          <option value="true">Blacklisted</option>
        </select>
        <i class="fa-solid fa-chevron-down absolute right-4 top-1/2 -translate-y-1/2 text-[10px] pointer-events-none text-gray-400"></i>
      </div>

      <!-- Sort Filter -->
      <div class="relative w-full md:w-40">
        <select id="sortFilter" class="w-full appearance-none bg-[#0a1425] border border-gray-800 rounded-lg px-4 py-3 pr-10 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 cursor-pointer">
          <option value="">Newest First</option>
          <option value="az">Name: A-Z</option>
          <option value="za">Name: Z-A</option>
        </select>
        <i class="fa-solid fa-chevron-down absolute right-4 top-1/2 -translate-y-1/2 text-[10px] pointer-events-none text-gray-400"></i>
      </div>
      <a href="/admin/vendors/pending"
class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition-all inline-flex items-center">
<i class="fa-solid fa-clock mr-2"></i>
Pending Vendors
</a>
    </div>
  </div>

  <!-- Table Container -->
  <div class="table-container overflow-x-auto rounded-xl border border-gray-800">
    <table class="w-full text-left border-collapse min-w-[1000px]">
      <thead>
        <tr class="bg-blue-600 text-white uppercase text-xs font-semibold tracking-wider">
          <th class="px-6 py-4 border-r border-blue-500/30">Brand Name</th>
          <th class="px-6 py-4 border-r border-blue-500/30">Phone</th>
          <th class="px-6 py-4 border-r border-blue-500/30">Email</th>
          <th class="px-6 py-4 border-r border-blue-500/30">Status</th>
          <th class="px-6 py-4">Block/Unblock</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-800" id="vendorTableBody">
        <% vendors.forEach((vendor) => { %>
          <tr class="bg-[#050a14] hover:bg-gray-800/50 transition-colors text-sm vendor-row" data-vendor-id="<%= vendor._id %>">
            <td class="px-6 py-4 border-r border-gray-800">
              <div class="flex items-center space-x-3">
                <div class="brand-avatar">
                  <%= vendor.brandName.charAt(0).toUpperCase() %>
                </div>
                <span class="font-medium vendor-name"><%= vendor.brandName %></span>
              </div>
            </td>
            <td class="px-6 py-4 border-r border-gray-800 vendor-phone"><%= vendor.phone || 'N/A' %></td>
            <td class="px-6 py-4 border-r border-gray-800 vendor-email text-gray-300"><%= vendor.brandEmail %></td>
            <td class="px-6 py-4 border-r border-gray-800">
              <span class="status-badge vendor-status <%= vendor.isBlocked ? 'status-blacklisted' : 'status-active' %>">
                <%= vendor.isBlocked ? 'Blacklisted' : 'Active' %>
              </span>
            </td>
            <td class="px-6 py-4">
              <!-- Block/Unblock Toggle -->
              <div class="flex items-center">
                <label class="relative inline-flex items-center cursor-pointer group">
                  <input type="checkbox" 
                         class="sr-only peer toggle-block-checkbox" 
                         data-vendor-id="<%= vendor._id %>"
                         <%= vendor.isBlocked ? 'checked' : '' %>>
                  <div class="w-11 h-6 bg-emerald-500 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full rtl:peer-checked:after:-translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:start-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-red-500"></div>
                </label>
              </div>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>

  <!-- Pagination -->
  <div class="flex flex-col sm:flex-row items-center justify-between pt-4 gap-4">
    <p class="text-sm text-gray-400">
      Showing <span id="paginationStart">1</span>-<span id="paginationEnd">10</span> from <span id="paginationTotal">0</span>
    </p>
    <div class="flex items-center space-x-2" id="paginationContainer">
      <button class="w-8 h-8 flex items-center justify-center rounded-lg bg-gray-800 hover:bg-gray-700 transition-colors text-xs" id="prevBtn">
        <i class="fa-solid fa-chevron-left"></i>
      </button>
      <div id="pageButtons"></div>
      <button class="w-8 h-8 flex items-center justify-center rounded-lg bg-gray-800 hover:bg-gray-700 transition-colors text-xs" id="nextBtn">
        <i class="fa-solid fa-chevron-right"></i>
      </button>
    </div>
  </div>
</div>

<% block('scripts', `
<script>
  // Initialize from server data
  var currentPage = ${locals.currentPage || 1};
  var totalVendors = ${locals.totalVendors || 0};
  var itemsPerPage = 10;
  var totalPages = Math.ceil(totalVendors / itemsPerPage);

  // Initialize
  document.addEventListener('DOMContentLoaded', function() {
    attachToggleListeners();
    attachFilterListeners();
    updatePaginationInfo();
  });

  // Attach toggle listeners
  function attachToggleListeners() {
    document.querySelectorAll('.toggle-block-checkbox').forEach(checkbox => {
      checkbox.addEventListener('change', handleToggleBlock);
    });
  }

  // Handle toggle block/unblock
  async function handleToggleBlock(event) {
    const checkbox = event.target;
    const vendorId = checkbox.getAttribute('data-vendor-id');
    const isBlocked = checkbox.checked;
    const row = checkbox.closest('.vendor-row');
    const vendorName = row.querySelector('.vendor-name').textContent;
    
    // Revert checkbox state immediately, we'll set it after confirmation
    checkbox.checked = !isBlocked;

    const result = await Swal.fire({
      title: isBlocked ? 'Block Vendor?' : 'Unblock Vendor?',
      text: 'Are you sure you want to ' + (isBlocked ? 'block' : 'unblock') + ' ' + vendorName + '?',
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: isBlocked ? '#ef4444' : '#22c55e',
      cancelButtonColor: '#374151',
      confirmButtonText: isBlocked ? 'Yes, Block' : 'Yes, Unblock'
    });

    if (!result.isConfirmed) return;

    // Set checkbox to desired state during request
    checkbox.checked = isBlocked;
    checkbox.disabled = true;

    try {
      const url = "/admin/vendors/" + vendorId + "/toggle-block";
      
      const response = await fetch(url, {
        method: 'PATCH',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          isBlocked: isBlocked
        })
      });

      const data = await response.json();

      if (data.success) {
        // Update status badge
        const statusBadge = row.querySelector('.vendor-status');
        statusBadge.textContent = isBlocked ? 'Blacklisted' : 'Active';
        statusBadge.className = "status-badge vendor-status " + (isBlocked ? 'status-blacklisted' : 'status-active');
        
        Swal.fire({
          icon: 'success',
          title: 'Success',
          text: data.message,
          timer: 1500,
          showConfirmButton: false,
          background: '#0a1425',
          color: '#fff'
        });
      } else {
        // Revert toggle on error
        checkbox.checked = !isBlocked;
        Swal.fire({
          icon: 'error',
          title: 'Error',
          text: data.message || 'Failed to update status',
          background: '#0a1425',
          color: '#fff'
        });
      }
    } catch (error) {
      // Revert toggle on error
      checkbox.checked = !isBlocked;
      Swal.fire({
        icon: 'error',
        title: 'Network Error',
        text: 'Failed to update vendor status. Please try again.',
        background: '#0a1425',
        color: '#fff'
      });
    } finally {
      // Re-enable checkbox
      checkbox.disabled = false;
    }
  }

  // Attach filter listeners
  function attachFilterListeners() {
    document.getElementById('searchInput').addEventListener('keyup', debounce(applyFilters, 500));
    document.getElementById('statusFilter').addEventListener('change', applyFilters);
    document.getElementById('sortFilter').addEventListener('change', applyFilters);
  }

  // Debounce function for search
  function debounce(func, delay) {
    var timeoutId;
    return function(...args) {
      clearTimeout(timeoutId);
      timeoutId = setTimeout(() => func.apply(this, args), delay);
    };
  }

  // Apply filters and reload data
  async function applyFilters() {
    const search = document.getElementById('searchInput').value;
    const status = document.getElementById('statusFilter').value;
    const sort = document.getElementById('sortFilter').value;
    
    currentPage = 1;
    
    try {
      const params = new URLSearchParams();
      if (search) params.append('search', search);
      if (status !== '') params.append('status', status);
      if (sort) params.append('sort', sort);
      params.append('page', currentPage);

      const response = await fetch("/admin/vendors?" + params.toString());
      const html = await response.text();
      
      // Parse the response and extract the table body
      const parser = new DOMParser();
      const doc = parser.parseFromString(html, 'text/html');
      const newTableBody = doc.getElementById('vendorTableBody');
      
      document.getElementById('vendorTableBody').innerHTML = newTableBody.innerHTML;
      
      // Extract totalVendors from the response
      const scriptMatch = html.match(/var totalVendors = (\\d+);/);
      if (scriptMatch) {
        totalVendors = parseInt(scriptMatch[1]);
        totalPages = Math.ceil(totalVendors / itemsPerPage);
      }
      
      attachToggleListeners();
      updatePaginationInfo();
    } catch (error) {
      console.error('Error applying filters:', error);
      alert('Failed to apply filters. Please try again.');
    }
  }

  // Update pagination
  function updatePaginationInfo() {
    const pageButtons = document.getElementById('pageButtons');
    
    // Calculate start and end
    const start = totalVendors > 0 ? ((currentPage - 1) * itemsPerPage) + 1 : 0;
    const end = Math.min(currentPage * itemsPerPage, totalVendors);
    
    document.getElementById('paginationStart').textContent = start;
    document.getElementById('paginationEnd').textContent = end;
    document.getElementById('paginationTotal').textContent = totalVendors;
    
    // Generate page buttons
    pageButtons.innerHTML = '';
    
    if (totalPages === 0) {
      return; // Don't show pagination if no results
    }
    
    const maxVisiblePages = 5;
    var startPage = Math.max(1, currentPage - Math.floor(maxVisiblePages / 2));
    var endPage = Math.min(totalPages, startPage + maxVisiblePages - 1);
    
    if (endPage - startPage < maxVisiblePages - 1) {
      startPage = Math.max(1, endPage - maxVisiblePages + 1);
    }
    
    for (var i = startPage; i <= endPage; i++) {
      const btn = document.createElement('button');
      const pageNum = i;
      btn.className = "w-8 h-8 flex items-center justify-center rounded-lg font-bold text-xs transition-colors " + (pageNum === currentPage ? 'bg-blue-600 text-white' : 'bg-white text-black hover:bg-gray-200');
      btn.textContent = pageNum;
      btn.addEventListener('click', function() {
        goToPage(pageNum);
      });
      pageButtons.appendChild(btn);
    }
    
    if (endPage < totalPages) {
      const span = document.createElement('span');
      span.className = 'px-2 text-gray-500';
      span.textContent = '...';
      pageButtons.appendChild(span);
    }

    // Update prev/next button state
    const prevBtn = document.getElementById('prevBtn');
    const nextBtn = document.getElementById('nextBtn');
    
    prevBtn.disabled = currentPage === 1;
    nextBtn.disabled = currentPage >= totalPages;
    
    if (prevBtn.disabled) {
      prevBtn.classList.add('opacity-50', 'cursor-not-allowed');
    } else {
      prevBtn.classList.remove('opacity-50', 'cursor-not-allowed');
    }
    
    if (nextBtn.disabled) {
      nextBtn.classList.add('opacity-50', 'cursor-not-allowed');
    } else {
      nextBtn.classList.remove('opacity-50', 'cursor-not-allowed');
    }
  }

  // Go to page
  function goToPage(page) {
    if (page === currentPage || page < 1 || page > totalPages) {
      return;
    }
    
    const search = document.getElementById('searchInput').value;
    const status = document.getElementById('statusFilter').value;
    const sort = document.getElementById('sortFilter').value;
    
    const params = new URLSearchParams();
    if (search) params.append('search', search);
    if (status !== '') params.append('status', status);
    if (sort) params.append('sort', sort);
    params.append('page', page);

    window.location.href = "/admin/vendors?" + params.toString();
  }

  // Previous button
  document.getElementById('prevBtn').addEventListener('click', function() {
    if (currentPage > 1) {
      goToPage(currentPage - 1);
    }
  });

  // Next button
  document.getElementById('nextBtn').addEventListener('click', function() {
    if (currentPage < totalPages) {
      goToPage(currentPage + 1);
    }
  });
</script>
`) %>